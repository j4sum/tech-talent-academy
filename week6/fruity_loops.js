const fruits = ["Apple", "Orange", "Banana", "Pear", "Peach", "Strawberry", "Cherry", "Acai"];

let fruit = "";
let i;

for (let fruit in fruits, vowels = 0, consonants = 0) {
    fruit += fruits[fruit];

    for (i = 0; i < fruit.length; i++) {
        // fruits[fruit[i]];
        if (fruit.length > 1) {
            arrayOfitems = [];
        }
    }
    fruits[fruit[i]];

}
console.log(vowels, consonants);